<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baymax.language_app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseLevel"
            android:label="@string/title_activity_choose_level" >
        </activity>
        <activity
            android:name=".DrawingCanvas"
            android:label="@string/title_activity_drawing_canvas" >
        </activity>
        <activity
            android:name=".Lessons"
            android:label="@string/title_activity_lessons" >
        </activity>
    </application>

</manifest>
